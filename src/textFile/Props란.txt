값을 전달할 수 있음

단, 부모가 자신한테만 전달할 수 있음
(형제 컴포넌트 끼리 공유 불가)
공유할 변수는 부모컴포넌트에서 생성해야함.

------------------
형태 : 객체
-> 비구조화 할당 문법을 사용하면 더 간결한 코드 사용 가능
사용전 : props.name
사용후 :
Left1({name}){ ✅ 비구조화 할당
  console.log(name);
}
